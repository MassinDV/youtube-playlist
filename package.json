{
    "name": "youtube-playlist",
    "version": "0.0.1",
    "description": "update videos url in a playlist to open a video without the playlist",
    "scripts": {
        "fixer": "npx prettier --write ."
    },
    "devDependencies": {
        "prettier": "^2.3.2"
    }
}
